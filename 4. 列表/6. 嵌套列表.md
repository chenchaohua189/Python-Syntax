## 1. 嵌套列表的初始化
### (1) 直接初始化
```python
a = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

print(a)    # [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
```

### (2) 使用for...in初始化
```python
a = [[(i * 3 + j + 1) for j in range(3)] for i in range(3)]

print(a)    # [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
```

## 2. 访问嵌套列表元素(参照访问列表元素)
## 3. 嵌套列表的切片(参照列表的切片)
## 4. 嵌套列表的运算(参照列表的运算)
## 5. 嵌套列表的复制(参照列表的复制)


| 运算符 | 描述 |
| - | - |
| `&` | 按位与 | 
| `\|` | 按位或 | 
| `^` | 按位异或 | 
| `~` | 取反 | 
| `<<` | 左移 |
| `>>` | 右移 |

```python
a = 1
b = 2

print(a & b)    # 0
print(a | b)    # 3
print(a ^ b)    # 3
print(~a)   # -2
print(a << 1)  # 2
print(a >> 1)   # 0
```

### 1. 值传递(int、float、bool、字符串、元组等):  将实参的初始值拷贝给形参。此时，对形参的改动不会影响实参的初始值
```python
def f(x):
    x = 2

x = 1

f(x)

print(x)  # 1
```
  
### 2. 对象传递(列表、集合、字典等): 将实参的引用传给形参，此时对形参的修改会影响实参的初始值
```python
def f(a):
    a[2:] = []

a = [1, 2, 3]

f(a)

print(a)    # [1, 2]
```

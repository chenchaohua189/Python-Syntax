### 1. 函数内定义的变量为局部变量，只能在函数内部使用(if、while块内变量可以在外部使用)
```python
def f():
    x = 2

print(x) # 编译错误, x没有定义 
```

### 2. global关键字: 当需要修改用全局变量时，需要用global关键字在函数内声明全局变量。
```python
x = 1

def f():
    global x

    x = 2

f()

print(x) # 2
```
